# Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Arena.social Session Cookie (from browser)
# 1. Login to https://arena.social with X
# 2. Open DevTools > Application > Cookies
# 3. Copy session cookie value
ARENA_SESSION_COOKIE=your_session_cookie_here

# Optional: API Token (if available)
ARENA_API_TOKEN=your_api_token_here
